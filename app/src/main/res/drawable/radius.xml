<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <layer-list>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="#FF0000" /> <!-- Color de fondo cuando se presiona -->
                    <corners android:radius="8dp" /> <!-- Radio de las esquinas -->
                </shape>
            </item>
            <item android:right="1dp" android:left="1dp" android:top="1dp" android:bottom="1dp">
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/transparent" /> <!-- Color de borde transparente -->
                    <stroke android:color="#FF0000" android:width="2dp" /> <!-- Color y ancho del borde -->
                    <corners android:radius="8dp" /> <!-- Radio de las esquinas -->
                </shape>
            </item>
        </layer-list>
    </item>
    <item>
        <layer-list>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/white" /> <!-- Color de fondo predeterminado -->
                    <corners android:radius="8dp" /> <!-- Radio de las esquinas -->
                </shape>
            </item>
            <item android:right="1dp" android:left="1dp" android:top="1dp" android:bottom="1dp">
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/transparent" /> <!-- Color de borde transparente -->
                    <stroke android:color="#FF0000" android:width="2dp" /> <!-- Color y ancho del borde -->
                    <corners android:radius="8dp" /> <!-- Radio de las esquinas -->
                </shape>
            </item>
        </layer-list>
    </item>
</selector>